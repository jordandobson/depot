<h1>Listing users</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th>Name</th>
  </tr>

<% for user in @users %>
  <tr>
    <td><%= link_to '&times;', user, :confirm => 'Are you sure?', :method => :delete %></td>
    <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <td><%=h user.name %></td>
  </tr>
<% end %>
</table>
<br />
<%= link_to 'New user', new_user_path %>
